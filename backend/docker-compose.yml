services:
  app:
    image: unbeatablebann/ai-interviewer
    container_name: fastapi_app
    ports:
      - "8001:8001"
    depends_on:
      - redis
    env_file:
      -  .env

  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"

